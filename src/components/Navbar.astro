<section class="">
    <nav class="w-full flex justify-center fixed bottom-5 z-50">
        <div class="w-[90%] max-w-[35rem] h-16 flex justify-between items-center font-semibold border rounded-full px-6 bg-pink-100 shadow-md relative">
      
          <!-- Logo -->
          <span class="font-bold text-lg">Adeleeeeyyyy</span>
      
          <!-- Hamburger (Mobile only) -->
          <button id="hamburger-btn" class="ml-auto">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none"
              viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M4 6h16M4 12h16M4 18h16" />
            </svg>
          </button>
      
          <!-- Mobile Dropdown Menu -->
          <div id="mobile-menu"
            class="absolute bottom-full px-2 mb-4 right-0 w-56 rounded-xl bg-pink-200 shadow-lg p-4 flex flex-col items-start gap-3 text-sm 
            transition-all duration-500 ease-in-out origin-bottom scale-75 opacity-0 pointer-events-none"
          >
            <ul class="flex justify-between items-center w-full">
                <a href="/" class="w-full hover:text-pink-600 text-lg">Projects</a>
                <span class="material-symbols-outlined">
                    code
                </span>
            </ul>
            <ul class="flex justify-between items-center w-full">
                <a href="/about" class="w-full hover:text-pink-600 text-lg">Certification</a>
                <span class="material-symbols-outlined">
                    card_membership
                </span>
            </ul>
            <ul class="flex justify-between items-center w-full">
                <a href="/project" class="w-full hover:text-pink-600 text-lg">Stats</a>
                <span class="material-symbols-outlined">
                    query_stats
                </span>
            </ul>
          </div>
        </div>
      </nav>
      
</section>

<script is:browser>
    const btn = document.getElementById("hamburger-btn");
    const menu = document.getElementById("mobile-menu");
  
    btn.addEventListener("click", () => {
      menu.classList.toggle("scale-75");
      menu.classList.toggle("opacity-0");
      menu.classList.toggle("pointer-events-none");
      menu.classList.toggle("scale-100");
      menu.classList.toggle("opacity-100");
    });
  </script>